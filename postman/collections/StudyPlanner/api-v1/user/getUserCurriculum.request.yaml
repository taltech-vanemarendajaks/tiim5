$kind: http-request
name: getUserCurriculum
description: This response should probably include modules and probably the
  courses too if applicable (vabaained oleks tühi list i imagine).
url: "{{host}}/users/:userExternalId/curriculum"
method: GET
pathVariables:
  userExternalId: ""
order: 5000
